{
  "name": "service-name",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "invoke:hello": "serverless invoke local --function hello",
    "testOnly": "serverless invoke test --compilers js:@babel/register --root .webpack/service",
    "test": "serverless webpack && serverless invoke test --compilers js:@babel/register --root .webpack/service"
  },
  "dependencies": {
    "@useful/mongo": "^0.1.5"
  }
}
